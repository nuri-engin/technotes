/**
 * Role Object / Enum
 * 
 * The role object defines the all the roles in the example application, I created it to use like an enum to avoid passing roles around as strings, 
 * so instead of 'Admin' and 'User' we can use Role.Admin and Role.User.
 */
module.exports = {
    Admin: 'Admin',
    User: 'User'
}